{
  "name": "job-scheduler-ui",
  "version": "1.0.0",
  "description": "Frontend for Job Scheduler API",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./src/output.css",
    "watch": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.0"
  }
}
